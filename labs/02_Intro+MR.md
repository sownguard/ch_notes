# Lab №1 (Introduction + MergeTree)

## Install
1.1 Установите инстанс clickhouse на локальную / виртуальную машину 
1.2 Установите подключение к БД через Dbeaver / DataGrip / аналоги 

## Intro. Testing external datasets with s3
[Вспоминаем SQL](https://clickhouse.com/docs/sql-reference/statements/select), если забыли 

Используйте публичный датасет

Описание полей можно получить запросом
```sql
DESCRIBE s3('https://datasets-documentation.s3.eu-west-3.amazonaws.com/pypi/2023/pypi_0_7_34.snappy.parquet');
```

2.1 Напишите запрос, возвращающий первые 10 строк датасета (limit)

2.2 Сколько строк в датасете? (count)

2.3 Создайте таблицу для хранения подмножества датасета:
 - произвольный нейминг
 - содержит колонки TIMESTAMP, COUNTRY_CODE, URL, PROJECT (Типы данных можно получить через DESCRIBE - Nullable можно игнорировать)
 - движок - MergeTree
 - PK - TIMESTAMP

2.4 Вставьте все строки из датасета в созданную таблицу [insert into ... select from](https://clickhouse.com/docs/integrations/s3)

(Работаем с созданной таблицей)

2.5 Выведите топ-100 самых загружаемых проектов (count / group by / order by)

2.6 Сколько строк было прочитано из таблицы для подсчета рез-та? (Ответ мб в рез-тах запроса)

2.7 Перепишите запрос из п2.5 - в запрос должны попасть только загрузки, выоплненные в апреле 2023 года. (Можно воспользоваться функциями [toStartOfMonth()](https://clickhouse.com/docs/sql-reference/functions/date-time-functions#toStartOfMonth), [toDate()](https://clickhouse.com/docs/sql-reference/functions/type-conversion-functions#toDate))

2.8 Сколько строк было прочитано запросом из п2.7? Почему прочитано меньше строк, чем запросом п2.5? Почему не весь датасет?

2.9 Напишите запрос - должен вывести кол-во загрузок python пакетов, названия которых начинаются с "boto". Конструкция LIKE используется для частичного сравнения.

2.10 Почему запрос п2.9 читает все строки таблицы?

2.11 Поэкспериментируем с PK

Создайте таблицу аналогичную созданной в п2.3, добавьте в PK поле PROJECT
```sql
CREATE TABLE xxx2(
    ...
)
ENGINE = MergeTree
PRIMARY KEY (TIMESTAMP, PROJECT);
```

Наполните таблицу данными
Выполните запрос аналогичный п2.9 для новой таблицы

2.12 Сколько гранил пропущено благодаря добавлению PROJECT в PK?

2.13 Если мы ожидаем частых запросов с фильтрацией по PROJECT - это поле стоит добавить в PK ДО TIMESTAMP (вспоминаем про кардинальность полей в PK)

Пересоздаейте таблицу п2.11 с PK 
```sql
PRIMARY KEY (PROJECT, TIMESTAMP);
```

Наполните ее данными

Выполните запрос п2.9

Сколько гранул было обработано?
(референсное значение 81920 rows, +- 2 гранулы)

Почему теперь нет фуллскана таблицы?


## Intro. Work with data parts

3.1. Удалите и создайте заново таблицу по примеру из скрипта. 

Вставляя за один раз в нее по 1000 строк, определите, после какой вставки произойдет первое слияние частей данных и после какой вставки произойдет второе слияние.

Напишите запрос, с помощью которого вы сможете понять, произошло ли слияние данных или нет.

В качестве ответа пришлите 2 скриншота вашего запроса и его результатов, по которым видно, что произошло 1ое и 2ое слияния частей данных.

3.2. Создайте таблицу по примеру async_test, как в уроке, и воспроизведите ассинхронную вставку в нее.

Пришлите скриншота запроса

```sql
SELECT * FROM system.asynchronous_insert_log;
```

, который вернет 1 или более строк (что будет говорить о том, что асинхронное объединение двух или более наборов строк произошло в буфере до создания part)

3.3. Объедините все части данных таблицы async_test в один. В качестве ответа приложите запрос, который производит данную операцию.
